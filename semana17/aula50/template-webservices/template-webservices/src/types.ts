export type user = {
   id: string
   name: string
   nickname: string
   email: string
   address: string
}

export type addressInfo = {
   id: string
   cep: string
   number: string
   complement: string
   street: string
   neighborhood: string
   city: string
   state: string
}